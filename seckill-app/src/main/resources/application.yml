server:
  port: 8080

spring:
  application:
    name: seckill-lab
  profiles:
    default: v0

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

logging:
  level:
    root: info
